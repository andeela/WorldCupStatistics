<UserControl x:Class="WorldCupWPF.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorldCupWPF.Views"
             mc:Ignorable="d" 
             FontSize="24"
             FontFamily="Cambria Math"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <Image 
            Grid.Column="0"
            Source= "C:\Users\anđela\source\repos\OOP.NET\WorldCupStatistics\DAL\Images\playerIcon.png"/>
        <!--ako player ima postavljenu sliku ucitaj nju, ako ne postavi ikonu iz dala-->

        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="0 100 0 0">
            <!--dodaj prave podatke o igracu-->
            <Border BorderThickness="1" BorderBrush="Black" Padding="10">
                <TextBlock Text="Luka MODRIĆ (C)"/>
            </Border>
            <TextBlock Text="10 - midfield" Margin="0 5 0 0"/>
            <TextBlock Text="Goals: "/>
            <TextBlock Text="Yellow cards:"/>
        </StackPanel>
    </Grid>
</UserControl>
